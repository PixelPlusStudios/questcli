name: quest
description: A gamified command-line traacker application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.4.2
  sqlite3: ^3.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  quest: main